<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.coddev.applu"
    >

    <application>
        <receiver
            android:name=".p.UninstallWidgetSBlack"
            android:label="Applu S black" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
             </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_provider_s_theme_black" />
        </receiver>
        <receiver
            android:name=".p.UninstallWidgetSWhite"
            android:label="Applu S white" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_provider_s_theme_white" />
        </receiver>
        <receiver
            android:name=".p.UninstallWidgetMBlack"
            android:label="Applu M black" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_provider_m_theme_black" />
        </receiver>
        <receiver
            android:name=".p.UninstallWidgetMWhite"
            android:label="Applu M white" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_provider_m_theme_white" />
        </receiver>

        <receiver
            android:name=".p.UninstallWidgetLBlack"
            android:label="Applu L black" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_provider_l_theme_black" />
        </receiver>
        <receiver
            android:name=".p.UninstallWidgetLWhite"
            android:label="Applu L white" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_provider_l_theme_white" />
        </receiver>
    </application>

</manifest>
